import { Center, Container } from '@chakra-ui/react'
import { useTheme } from '@emotion/react'
import React from 'react'

const Error = () => {
    const theme = useTheme()

    return <Center>Page Not Found lol</Center>
}

export default Error
